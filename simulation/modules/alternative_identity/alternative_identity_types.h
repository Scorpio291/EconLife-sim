#pragma once

// alternative_identity module types.
// Module-specific types for the alternative_identity module (Tier 9).
// Core shared types are in their respective core headers.

#include <cstdint>
#include <string>
#include <vector>

namespace econlife {

// Placeholder — types will be defined during implementation based on INTERFACE.md.

}  // namespace econlife
