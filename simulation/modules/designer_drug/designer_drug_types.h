#pragma once

// designer_drug module types.
// Module-specific types for the designer_drug module (Tier 9).
// Core shared types are in their respective core headers.

#include <cstdint>
#include <string>
#include <vector>

namespace econlife {

// Placeholder — types will be defined during implementation based on INTERFACE.md.

}  // namespace econlife
